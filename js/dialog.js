$(function () {
	$("#dialog").dialog({
		autoOpen: false,
		// modal: true,
		position: {
			my: "center+42 top",
			at: "top-84",
			of: "#contact_form"
		}
	});
	// $(".submit-button").click(function () {
	// 	$("#dialog").dialog("open");
	// });
});