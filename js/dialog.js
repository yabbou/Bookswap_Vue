$(function () {
	$("#dialog").dialog({
		autoOpen: false,
		// modal: true,
		position: {
			my: "center+42 top",
			at: "top-84",
			of: "form"
		}
	});
	// $(".submit-button").click(function () {
	// 	$("#dialog").dialog("open");
	// });
});